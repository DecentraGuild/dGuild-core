/**
 * Tracker address book (alias for useAddressBook — tenant_mint_catalog via tenant_catalog edge).
 * Kept as separate composable for explicit module boundary.
 */
export { useAddressBook as useTrackerAddressBook } from '~/composables/watchtower/useAddressBook'
