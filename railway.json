{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm exec turbo run build --filter=api --filter=discord-bot"
  },
  "deploy": {
    "startCommand": "sh -lc \"case \\\"$SERVICE_ROLE\\\" in worker) pnpm --filter api worker ;; bot) pnpm --filter discord-bot start ;; *) pnpm --filter api start ;; esac\""
  }
}
