{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm exec turbo run build --filter=api"
  },
  "deploy": {
    "startCommand": "sh -c 'case \"$SERVICE_ROLE\" in worker) node apps/api/dist/worker.js ;; *) node apps/api/dist/index.js ;; esac'"
  }
}
