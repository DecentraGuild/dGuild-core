{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm exec turbo run build --filter=api"
  },
  "deploy": {
    "startCommand": "sh -lc \"if [ \\\"$SERVICE_ROLE\\\" = worker ]; then pnpm --filter api worker; else pnpm --filter api start; fi\""
  }
}
