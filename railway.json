{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm exec turbo run build --filter=discord-bot"
  },
  "deploy": {
    "startCommand": "pnpm --filter discord-bot start"
  }
}
