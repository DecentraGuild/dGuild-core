{
  "name": "decentraguild",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 15",
    "lint": "pnpm exec eslint apps packages",
    "build:platform": "pnpm exec turbo run build --filter=./packages/* --filter=@decentraguild/config && pnpm --filter platform generate",
    "build:tenant": "pnpm exec turbo run build --filter=./packages/* --filter=./config && pnpm --filter tenant generate"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "eslint": "^10.0.0",
    "eslint-plugin-vue": "^10.0.0",
    "turbo": "^2.8.9",
    "typescript-eslint": "^8.0.0"
  }
}
